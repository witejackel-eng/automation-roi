{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "prisma generate && bash scripts/migrate-or-warn.sh && next build",
  "installCommand": "bun install",
  "regions": [
    "iad1"
  ]
}
